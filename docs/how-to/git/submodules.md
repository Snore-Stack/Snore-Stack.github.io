# Installer et mettre à jour les sous-modules Git

Lorsque vous clonez un projet contenant des sous-modules déclarés dans `.gitmodules`, utilisez la commande suivante pour les télécharger et les initialiser :

```bash
git submodule update --init --recursive
```
